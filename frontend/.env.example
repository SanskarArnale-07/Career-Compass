# API Base URL for backend scoring service (default: http://127.0.0.1:8000)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional: OpenAI API Key for AI Career Coach LLM responses (falls back to deterministic grounded engine if omitted)
OPENAI_API_KEY=
